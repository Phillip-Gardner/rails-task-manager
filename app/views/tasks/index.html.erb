<h1>Hello Mate</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> - <%= task.completed %></li>
  <% end %>
</ul>
<h3><%= link_to "Add a new task", new_task_path %></h3>
